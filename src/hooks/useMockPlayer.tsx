import React, { useEffect, useState } from 'react';

// Mock player two data
export const useMockPlayer = () => {
    const data: IPlayer = {
        uuid: 'uuid-b',
        username: 'Destroyer Ai'
    };
    
    return data;
};